import { ControlInjectorPipe } from './control-injector.pipe';

describe('ControlInjectorPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlInjectorPipe();
    expect(pipe).toBeTruthy();
  });
});
