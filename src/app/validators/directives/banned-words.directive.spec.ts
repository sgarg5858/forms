import { BannedWordsDirective } from './banned-words.directive';

describe('BannedWordsDirective', () => {
  it('should create an instance', () => {
    const directive = new BannedWordsDirective();
    expect(directive).toBeTruthy();
  });
});
